CMAKE_MINIMUM_REQUIRED(VERSION 3.0.2)

SET(HDC_SORUCES_HEADERS
    ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/include/SMBIOSType.h
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/baseboard.h
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/smbios.h
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/smbios_table.h
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/wmi.h
)

SET(HDC_SORUCES_FILES
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/baseboard.cpp
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/hw_collect.cpp
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/smbios.cpp
    # ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/wmi.cpp
)

SET(HDC_PLATFORM_INCLUDES)

SET(HDC_PLATFORM_SOURCES)

IF (MSVC)
    SET (HDC_PLATFORM_INCLUDES
        ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/windows/ComPtr.h
        ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/windows/SMBIOSTable.h
    )

    SET (HDC_PLATFORM_SOURCES
        ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/windows/SMBIOSTable.c
    )
ENDIF(MSVC)

CONCRETE_ADD_PROJECT(
    NAME libhdc
    TYPE "Execute"
    INCLUDE_FILES ${HDC_SORUCES_HEADERS} ${HDC_PLATFORM_INCLUDES}
    SOURCE_FILES  ${HDC_SORUCES_FILES} ${HDC_PLATFORM_SOURCES}
    INCLUDE_DIRECTORIES  ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/windows ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/include
)