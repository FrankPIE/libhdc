CMAKE_MINIMUM_REQUIRED(VERSION 3.0.2)

CONCRETE_ADD_PROJECT(
    NAME HardwareCollect
    TYPE "Execute"
    SOURCE_FILES  ${CMAKE_CURRENT_SOURCE_DIR}/main.c
    INCLUDE_DIRECTORIES ${CONCRETE_PROJECT_SOURCE_DIR}/${HDC_SORUCE_DIR}/include
)